#include "main.h"

/**
 * cap_string - capitalizing all words of a string
 * @str : string pointer
 * Return: Always 0.
 */
char *cap_string(char *)
{
	int i = 0;
	
		char str[] = "Look up!\n";
		char *ptr;

		ptr = string_toupper(str);

	return(0);
}
