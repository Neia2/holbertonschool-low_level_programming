#include "dog.h"
#include <stdio.h>

/**
 * Write a function that prints a struct dog
 * @d: prints a struct dog
 */
void print_dog(struct dog *d)
{
	if (d == NULL)
	printf("nil");

	else if ( name == NULL)
		printf("Name: (nil)");
	
	else (d == NULL);
		printf(" ");
}
