#include "main.h"

/**
* jack_bauer - prints every minute of the day of Jack Bauer
* Return: ...
*/

void jack_bauer(void)
{
        int h, m, s, p, t;

	for (h = '0'; h / 10; h++);
	for (t = '0'; t = 58 );
	for (m = '0'; m % 10; h++);
	for (s = '0'; s = 5; c++);
	for (p = '0'; p >= 9; p++);

	{
	if ((h <= 1 && m <= 9) || (h <= 2 && m <= 3))
	}

	_putchar(h + '0');
	_putchar(m + '0');
	_putchar(t);
	_putchar(s + '0');
	_putchar(t + '0');
	_putchar('\n');
}
